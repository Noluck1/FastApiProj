[tool.mypy]
python_version = "3.14"
plugins = ["pydantic.mypy"]

strict = true
warn_unreachable = true
show_error_codes = true
pretty = true

exclude = [
    "\\.venv",
    "venv",
]

[tool.pydantic-mypy]
init_typed = true
warn_required_dynamic_aliases = true